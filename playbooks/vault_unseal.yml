---
- name: "[VAULT] Unseal"
  hosts: "hashistack"
  become: true
  gather_facts: true

  tasks:
    - name: "Vault"
      include_role:
        name: "vault"
        tasks_from: "_inplace_init_unseal.yml"
        apply:
          tags:
            - vault

      tags:
        - vault

